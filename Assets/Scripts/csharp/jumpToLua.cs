using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class jumpToLua : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("CsTolua");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
