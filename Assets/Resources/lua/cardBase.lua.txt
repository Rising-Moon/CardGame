local Base = require("objectBase")

Card =base:new()
function Card:new(data)
    obj=userdata or Base:new(date)
    setmetatable(obj,self)
    self.__index = self
    return obj
end

function Card:showCardId()
    print("card's id is :"+self.objId)
end

return  Card